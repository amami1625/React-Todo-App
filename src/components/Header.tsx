import styled from "styled-components";

const HeaderElm = styled.header``;
const H1 = styled.h1``;

const Header = () => {
  return (
    <HeaderElm>
      <H1>My Todo App(β)</H1>
    </HeaderElm>
  );
};

export default Header;
